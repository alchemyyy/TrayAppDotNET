<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <!-- Match the main project's TFM exactly so referencing it doesn't trigger
         downlevel-compatibility shims. The main project is Avalonia/WinExe; this app
         only consumes its non-UI types (Models, Services). -->
    <TargetFramework>net10.0-windows10.0.22621.0</TargetFramework>
    <RootNamespace>BrightnessTrayAppDotNET.Tests</RootNamespace>
    <AssemblyName>SunShifterCheck</AssemblyName>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>

    <!-- Skip the repo-root Directory.Build.props BuildInfo codegen. The main project
         already emits its own BuildInfo class; a duplicate from this project would
         mostly waste a build-counter increment on Release. -->
    <EmitBuildInfo>false</EmitBuildInfo>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\BrightnessTrayAppDotNET.csproj"
                      Condition="'$(TrayAppDotNETAggregateBuild)' == 'true'"
                      Targets="GetTargetPath" />
    <ProjectReference Include="..\..\src\BrightnessTrayAppDotNET.csproj"
                      Condition="'$(TrayAppDotNETAggregateBuild)' != 'true'"
                      AdditionalProperties="SkipPublishAfterBuild=true;TrayAppDotNETAggregateBuild=false;TrayAppDotNETUseRootOutput=false;Platform=x64" />
  </ItemGroup>
</Project>
