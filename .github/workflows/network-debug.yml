name: Network Debug

on:
  push:
  pull_request:
    paths:
      - NetworkTrayAppDotNET/**
      - TrayAppDotNETCommon/**
      - Directory.Build.props
      - TrayAppDotNET.Parent.props
      - TrayAppDotNET.Parent.targets
      - .github/workflows/network-debug.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  debug:
    name: NetworkTrayAppDotNET Debug
    runs-on: [self-hosted, Windows, X64, trayapp]
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          # Submodules are checked out manually below.
          # actions/checkout updates submodules before stale self-hosted runner
          # config can be cleared.
          submodules: false

      - name: Checkout Submodules
        shell: powershell -NoProfile -ExecutionPolicy Bypass -File {0}
        run: |
          $ErrorActionPreference = "Stop"
          # Clear cached submodule config from reused self-hosted workspaces.
          # --checkout forces the recorded commit instead of merge/rebase.
          git submodule deinit --force --all
          git submodule sync --recursive
          git submodule update --init --force --checkout --depth=1 --recursive

      - name: Verify .NET SDK
        shell: powershell -NoProfile -ExecutionPolicy Bypass -File {0}
        run: |
          $ErrorActionPreference = "Stop"
          Get-Command dotnet
          dotnet --info
          dotnet --list-sdks | Select-String -Pattern '^10\.'

      - name: Restore
        shell: powershell -NoProfile -ExecutionPolicy Bypass -File {0}
        run: dotnet restore "NetworkTrayAppDotNET/src/NetworkTrayAppDotNET.csproj" --disable-parallel -p:Platform=x64 -p:EnableWindowsTargeting=true

      - name: Build Debug
        shell: powershell -NoProfile -ExecutionPolicy Bypass -File {0}
        run: >
          dotnet build "NetworkTrayAppDotNET/src/NetworkTrayAppDotNET.csproj"
          --configuration Debug
          --no-restore
          --output ".artifacts/debug/NetworkTrayAppDotNET"
          -p:Platform=x64
          -p:EnableWindowsTargeting=true
          # Debug artifacts are built for CI only, so avoid local app shutdowns
          # and publish side effects.
          -p:SkipKillRunningInstance=true
          -p:SkipPublishAfterBuild=true
          # Keep uploaded debug artifacts small.
          -p:DebugType=none
          -p:DebugSymbols=false
          -p:ContinuousIntegrationBuild=true

      - name: Remove PDB Files
        shell: powershell -NoProfile -ExecutionPolicy Bypass -File {0}
        run: |
          $ErrorActionPreference = "Stop"
          Get-ChildItem ".artifacts/debug/NetworkTrayAppDotNET" -Recurse -Filter *.pdb -File |
            Remove-Item -Force

      - name: Upload Debug Artifact
        uses: actions/upload-artifact@v7
        with:
          name: network-tray-app-debug-${{ github.sha }}
          path: .artifacts/debug/NetworkTrayAppDotNET/**
          if-no-files-found: error
          retention-days: 14
