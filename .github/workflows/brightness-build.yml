name: Brightness Build

on:
  push:
    branches: [main, master]
    paths:
      - BrightnessTrayAppDotNET/**
      - TrayAppDotNETCommon/**
      - Directory.Build.props
      - TrayAppDotNET.Parent.props
      - TrayAppDotNET.Parent.targets
      - .github/workflows/brightness-build.yml
      - .github/workflows/_trayapp-build.yml
  pull_request:
    paths:
      - BrightnessTrayAppDotNET/**
      - TrayAppDotNETCommon/**
      - Directory.Build.props
      - TrayAppDotNET.Parent.props
      - TrayAppDotNET.Parent.targets
      - .github/workflows/brightness-build.yml
      - .github/workflows/_trayapp-build.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/_trayapp-build.yml
    with:
      app_name: BrightnessTrayAppDotNET
      project_path: BrightnessTrayAppDotNET/src/BrightnessTrayAppDotNET.csproj
      artifact_slug: brightness-tray-app
