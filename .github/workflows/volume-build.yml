name: Volume Build

on:
  push:
    branches: [main, master]
    paths:
      - VolumeTrayAppDotNET/**
      - TrayAppDotNETCommon/**
      - Directory.Build.props
      - TrayAppDotNET.Parent.props
      - TrayAppDotNET.Parent.targets
      - .github/workflows/volume-build.yml
      - .github/workflows/_trayapp-build.yml
  pull_request:
    paths:
      - VolumeTrayAppDotNET/**
      - TrayAppDotNETCommon/**
      - Directory.Build.props
      - TrayAppDotNET.Parent.props
      - TrayAppDotNET.Parent.targets
      - .github/workflows/volume-build.yml
      - .github/workflows/_trayapp-build.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/_trayapp-build.yml
    with:
      app_name: VolumeTrayAppDotNET
      project_path: VolumeTrayAppDotNET/src/VolumeTrayAppDotNET.csproj
      artifact_slug: volume-tray-app
